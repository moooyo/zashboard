// This module belongs to main.ts's post-scrub application graph, so Vite can
// preload the one deterministic text stack without moving it ahead of the
// credential-scrubbing bootstrap boundary.
import 'subsetted-fonts/MiSans-VF/MiSans-VF.css'
